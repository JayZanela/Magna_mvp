# =============================================================================
# CONFIGURAÇÃO DE AMBIENTE - MAGNA MVP
# =============================================================================
# Este arquivo contém exemplos das variáveis de ambiente necessárias.
# Copie para .env.local e substitua pelos valores reais para desenvolvimento.

# =============================================================================
# DATABASE
# =============================================================================
# URL de conexão com o banco de dados SQLite
# Para desenvolvimento local, aponte para o arquivo dev.db na pasta database
DATABASE_URL="file:./database/dev.db"

# =============================================================================
# NEXT.JS AUTHENTICATION
# =============================================================================
# Chave secreta para NextAuth.js (se usado futuramente)
# Gere uma chave forte: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-key-minimum-32-characters-long"
NEXTAUTH_URL="http://localhost:3000"

# =============================================================================
# JWT AUTHENTICATION (OBRIGATÓRIO PARA MVP)
# =============================================================================
# Chave secreta para assinatura dos Access Tokens (15 minutos de duração)
# CRÍTICO: Use uma chave forte e única - mínimo 32 caracteres
# Comando para gerar: openssl rand -base64 32
JWT_SECRET="example-jwt-secret-change-this-to-secure-32char-key"

# Chave secreta para assinatura dos Refresh Tokens (7 dias de duração)
# CRÍTICO: Deve ser DIFERENTE do JWT_SECRET e igualmente forte
# Comando para gerar: openssl rand -base64 32
JWT_REFRESH_SECRET="example-refresh-secret-change-this-to-different-32char-key"

# =============================================================================
# CONFIGURAÇÕES DE SEGURANÇA
# =============================================================================
# Ambiente da aplicação (development, production)
NODE_ENV="development"

# =============================================================================
# INSTRUÇÕES DE USO:
# =============================================================================
# 1. Copie este arquivo para .env.local
# 2. Substitua TODOS os valores "example-*" por valores reais e seguros
# 3. Use o comando "openssl rand -base64 32" para gerar chaves seguras
# 4. NUNCA commite o arquivo .env.local no git
# 5. JWT_SECRET e JWT_REFRESH_SECRET devem ser diferentes entre si
# =============================================================================